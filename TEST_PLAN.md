# 統合テスト計画 — LINE Bot + 管理画面 MVP

## 1. テスト方針

### 対象バージョン
MVP段階3（STEP1〜5 実装完了）

### テスト種別
本ドキュメントは**手動統合テスト**の計画を扱う。  
単体テスト・E2Eテストフレームワークの導入はMVPスコープ外。

### テスト環境
| 項目 | 内容 |
|---|---|
| 実行環境 | `npm run dev`（ローカル）または Vercel プレビュー |
| DB | Supabase（開発プロジェクト） |
| LINE | LINE Developers コンソールのテスト用チャネル |
| Webhook 到達 | ngrok 等のトンネリングツールでローカルを公開 |

### 前提条件
- `.env.local` に全環境変数が設定済みであること
- Supabase の `faq` / `menus` / `conversations` テーブルが存在すること
- `LINE_OWNER_USER_ID` にオーナーの LINE ユーザーID が設定済みであること
- ngrok 等で `http://localhost:3000` を公開し、LINE Webhook URL に設定済みであること

---

## 2. テスト領域と優先度の考え方

| 領域 | 理由 |
|---|---|
| **高** | ユーザーへの直接影響・誤送信リスク・認証バイパスリスク |
| **中** | 機能正常系・UX に影響するが業務停止にはならない |
| **低** | エッジケース・UI の細部・将来的な回帰リスク |

---

## 3. テストケース一覧

凡例: ✅ PASS（実施済み）／ ⬜ 未実施

---

### A. FAQ 応答（Webhook → Claude API → LINE 返信）

| # | テストケース | 手順 | 期待結果 | 優先度 | 結果 |
|---|---|---|---|---|---|
| A-01 | **営業時間の質問に高確信度で回答** | FAQ に「営業時間」エントリを登録した状態で「営業時間を教えてください」と送信 | LINE にFAQ内容を元にした回答が返る。`conversations` に `confidence >= 5`・`escalated = false` で記録される | 高 | ✅ PASS |
| A-02 | **メニュー・料金の質問に回答** | 「カットの料金はいくらですか？」と送信 | 料金情報を含む回答が返る | 高 | ✅ PASS |
| A-03 | **アクセス・駐車場の質問に回答** | 「駐車場はありますか？」と送信 | アクセス情報を含む回答が返る | 高 | ✅ PASS |
| A-04 | **施術の組み合わせ可否に回答** | 「カットとカラーを同日に受けられますか？」と送信 | 施術組み合わせに関する回答が返る | 中 | ✅ PASS |
| A-05 | **予約案内に回答** | 「予約はどうすればいいですか？」と送信 | 予約に関する案内が返る | 中 | ✅ PASS |
| A-06 | **FAQに該当しない質問は低確信度になる** | FAQに全く関係のない「今日の天気は？」と送信 | オーナーにエスカレーション通知が届く。`conversations` に `confidence <= 4`・`escalated = true` で記録される | 高 | ✅ PASS |
| A-07 | **Webhook は 3 秒以内に 200 を返す** | LINE から任意のテキストを送信し、Webhook ログのレスポンス時間を確認 | LINE Developers コンソールの Webhook 送信結果で `200 OK` が記録され、タイムアウトエラーが発生しない | 高 | ✅ PASS |
| A-08 | **署名検証：不正なリクエストを拒否** | `x-line-signature` を改ざんしたPOSTを `/api/webhook` に送信 | `401 Unauthorized` が返る | 高 | ✅ PASS（curl で実機確認済み） |

---

### B. エスカレーション通知

| # | テストケース | 手順 | 期待結果 | 優先度 | 結果 |
|---|---|---|---|---|---|
| B-01 | **低確信度でオーナーにプッシュ通知が届く** | FAQに存在しない質問を送信し、`confidence <= 4` になることを確認 | オーナーの LINE に「【エスカレーション通知】」を含むメッセージが届く | 高 | ⬜ 未実施 |
| B-02 | **通知メッセージに質問内容・確信度・AI回答案が含まれる** | B-01 の通知メッセージ内容を確認 | 通知本文に「■ 質問内容」「■ 確信度」「■ AI回答案」の3セクションがある | 高 | ⬜ 未実施 |
| B-03 | **`LINE_OWNER_USER_ID` 未設定時でも処理が継続する** | `.env.local` から `LINE_OWNER_USER_ID` を除いてリスタート後、低確信度質問を送信 | サーバーログに `[escalation] LINE_OWNER_USER_ID is not set` が記録され、サーバーがクラッシュしない | 中 | ⬜ 未実施 |
| B-04 | **エスカレーション後も `conversations` に記録される** | B-01 の後、管理画面 `/admin/conversations` を確認 | 該当会話が `escalated = true`・赤バッジ付きで表示される | 高 | ⬜ 未実施 |
| B-05 | **エスカレーション件数がダッシュボードに反映される** | B-01 の後、`/admin` ダッシュボードを確認 | 「エスカレーション件数」が 1 増えている | 中 | ⬜ 未実施 |

---

### C. 管理画面 CRUD

#### C-1. 認証

| # | テストケース | 手順 | 期待結果 | 優先度 | 結果 |
|---|---|---|---|---|---|
| C-01 | **未認証で `/admin` にアクセスすると `/admin/login` にリダイレクト** | ブラウザで Cookie なしの状態で `/admin` を開く | `/admin/login` にリダイレクトされる | 高 | ✅ PASS |
| C-02 | **誤ったパスワードではログインできない** | `/admin/login` で間違ったパスワードを入力して送信 | エラーメッセージ「パスワードが正しくありません」が表示される。リダイレクトされない | 高 | ✅ PASS |
| C-03 | **正しいパスワードでログインできる** | 正しい `ADMIN_PASSWORD` を入力して送信 | `/admin` ダッシュボードにリダイレクトされる | 高 | ✅ PASS |
| C-04 | **Cookie は 7 日間有効** | ログイン後に DevTools でCookieを確認 | `admin_session` Cookie の Max-Age が `604800`（秒）であること | 中 | ✅ PASS |
| C-05 | **ログアウトで Cookie が削除される** | `/admin/logout` にアクセス後、再度 `/admin` を開く | `/admin/login` にリダイレクトされる | 高 | ✅ PASS |
| C-06 | **Cookie を手動削除すると認証が切れる** | DevTools で `admin_session` Cookie を削除後に `/admin/faqs` を開く | `/admin/login` にリダイレクトされる | 中 | ✅ PASS |

#### C-2. ダッシュボード

| # | テストケース | 手順 | 期待結果 | 優先度 | 結果 |
|---|---|---|---|---|---|
| C-07 | **統計 3 件が正しく表示される** | FAQ を数件登録・当日に質問を送信・エスカレーションを発生させた後に `/admin` を確認 | 「FAQ 件数」「本日の問い合わせ」「エスカレーション件数」が実際のデータと一致する | 中 | ⬜ 未実施 |
| C-08 | **ナビゲーションリンクが全て機能する** | ダッシュボードの4つのリンクを順番にクリック | 各ページ（/admin/faqs, /admin/conversations, /admin/broadcast, /admin/menus）に遷移する | 低 | ⬜ 未実施 |

#### C-3. FAQ 管理

| # | テストケース | 手順 | 期待結果 | 優先度 | 結果 |
|---|---|---|---|---|---|
| C-09 | **FAQ を新規作成できる** | `/admin/faqs/new` でフォームを入力して保存 | 一覧に追加される。次の LINE 質問でその FAQ が参照される | 高 | ✅ PASS |
| C-10 | **FAQ を編集できる** | 既存FAQの「編集」をクリックし内容を変更して更新 | 一覧の内容が変わる | 高 | ✅ PASS |
| C-11 | **FAQ を削除できる** | 削除ボタン → 確認ダイアログで OK | 一覧から削除される | 高 | ✅ PASS |
| C-12 | **削除確認ダイアログでキャンセルすると削除されない** | 削除ボタン → 確認ダイアログで「キャンセル」 | 一覧が変わらない | 中 | ✅ PASS |
| C-13 | **空フォームで保存するとバリデーションエラー** | 全フィールド空で「保存する」 | エラーメッセージが表示される。DBには保存されない | 高 | ✅ PASS |
| C-14 | **question 500文字超でエラー** | question に 501 文字を入力して送信 | 「質問は500文字以内で入力してください」エラーが表示される | 中 | ✅ PASS |

#### C-4. 会話ログ

| # | テストケース | 手順 | 期待結果 | 優先度 | 結果 |
|---|---|---|---|---|---|
| C-15 | **ユーザー単位で一覧表示される** | 複数回 LINE から送信後に `/admin/conversations` を確認 | 同一ユーザーは 1 行にまとまり、最終問い合わせ日時・最終メッセージが表示される | 高 | ✅ PASS |
| C-16 | **エスカレーションユーザーに赤バッジが表示される** | エスカレーションが発生したユーザーの一覧行を確認 | 「エスカレーション」赤バッジが付いている | 高 | ✅ PASS |
| C-17 | **行クリックで会話履歴詳細に遷移する** | 一覧の行をクリック | `/admin/conversations/[lineUserId]` に遷移し、会話が時系列昇順で表示される | 高 | ✅ PASS |
| C-18 | **詳細画面でエスカレーション済み会話にオレンジバッジが付く** | B-01 後に該当ユーザーの詳細画面を確認 | `escalated = true` の会話エントリに「エスカレーション済み」オレンジバッジが表示される | 中 | ✅ PASS |
| C-19 | **存在しない lineUserId で 404 になる** | `/admin/conversations/INVALID_USER_ID` にアクセス | 404 ページが表示される | 低 | ✅ PASS |

#### C-5. メニュー管理

| # | テストケース | 手順 | 期待結果 | 優先度 | 結果 |
|---|---|---|---|---|---|
| C-20 | **メニューを新規作成できる** | `/admin/menus/new` で name・price・description を入力して保存 | 一覧に追加される。price が `¥X,XXX` 形式で表示される | 高 | ⬜ 未実施 |
| C-21 | **メニューを編集できる** | 既存メニューの「編集」をクリックし price を変更して更新 | 一覧の price が変わる | 高 | ⬜ 未実施 |
| C-22 | **メニューを削除できる** | 削除ボタン → 確認ダイアログで OK | 一覧から削除される | 高 | ⬜ 未実施 |
| C-23 | **price に小数を入力するとバリデーションエラー** | price に `3500.5` を入力して送信 | 「料金は0以上の整数で入力してください」エラーが表示される | 中 | ⬜ 未実施 |
| C-24 | **price に負数を入力するとバリデーションエラー** | price に `-100` を入力して送信 | 「料金は0以上の整数で入力してください」エラーが表示される | 中 | ⬜ 未実施 |
| C-25 | **price に 0 を入力して登録できる** | price に `0` を入力して保存 | `¥0` として一覧に表示される | 低 | ⬜ 未実施 |

---

### D. お知らせ配信

| # | テストケース | 手順 | 期待結果 | 優先度 | 結果 |
|---|---|---|---|---|---|
| D-01 | **入力 → 確認 → 送信の 2 段階フローが機能する** | `/admin/broadcast` でメッセージ入力 → 「確認画面へ進む」 → 内容確認 → 「送信する」 | LINE に配信メッセージが届く。完了画面に「送信成功 N 件」が表示される | 高 | ✅ PASS |
| D-02 | **確認画面に送信対象人数・サンプルID・メッセージ本文が表示される** | D-01 の確認画面で内容を確認 | 送信対象人数・先頭 5 件のユーザーID・入力メッセージ本文がすべて表示されている | 高 | ✅ PASS |
| D-03 | **「キャンセル（戻る）」で入力画面に戻れる** | 確認画面で「キャンセル（戻る）」をクリック | 入力画面に戻る。LINE には何も送信されていない | 高 | ✅ PASS |
| D-04 | **空メッセージでは確認画面に進めない** | メッセージ未入力で「確認画面へ進む」をクリック | エラーメッセージ「メッセージを入力してください」が表示される | 高 | ✅ PASS |
| D-05 | **重複ユーザーは 1 回しか送信されない** | 同一ユーザーが複数回 LINE 送信している状態で配信を実行 | 送信成功件数 = 重複排除後のユニークユーザー数 | 高 | ✅ PASS |
| D-06 | **1 件失敗しても他ユーザーへの送信が継続する** | 存在しない LINE ユーザーID が conversations に混在する状態で配信 | 完了画面に「送信失敗 N 件」が記録され、他ユーザーへの送信は成功する | 中 | ✅ PASS |
| D-07 | **送信完了後「別のメッセージを送る」で入力画面にリセットされる** | 完了画面で「別のメッセージを送る」をクリック | 入力画面に戻り、前のメッセージがクリアされている | 中 | ⬜ 未実施 |

---

### E. エッジケース

| # | テストケース | 手順 | 期待結果 | 優先度 | 結果 |
|---|---|---|---|---|---|
| E-01 | **空文字（改行のみ）のメッセージを送信** | LINE から空白・改行のみのメッセージを送信 | Bot が応答するか、処理がクラッシュしない。`conversations` への記録は残る | 中 | ✅ PASS |
| E-02 | **長文（1000文字超）のメッセージを送信** | 1000 文字以上のテキストを LINE から送信 | Bot が応答するか、タイムアウトしない | 中 | ✅ PASS |
| E-03 | **絵文字を含むメッセージを送信** | 「営業時間🕐を教えてください」と送信 | FAQ を参照した回答が返る。文字化けしない | 低 | ✅ PASS |
| E-04 | **スタンプを送信** | LINE からスタンプを送信 | テキストメッセージ以外は無視される（応答なし・エラーなし） | 中 | ✅ PASS |
| E-05 | **画像を送信** | LINE から画像ファイルを送信 | テキスト以外は無視される（応答なし・エラーなし） | 低 | ✅ PASS |

---

## 4. テスト実行順序（推奨）

```
1. 環境準備（ngrok 起動・Webhook URL 設定）
2. C-01〜C-06（認証基盤の確認）
3. A-08（署名検証）
4. C-09〜C-11（FAQ 登録・編集・削除）
5. A-01〜A-06（FAQ 応答確認）← FAQが登録された状態で実施
6. A-07（応答時間確認）
7. B-01〜B-05（エスカレーション通知）
8. C-15〜C-18（会話ログ確認）← 上記で会話が蓄積された状態で実施
9. D-01〜D-07（お知らせ配信）
10. C-07（ダッシュボード統計）← データが揃った状態で確認
11. C-20〜C-25（メニュー管理）
12. E-01〜E-05（エッジケース）
```

---

## 5. 優先度・実施状況サマリー

> **注記**: 当初サマリーに「合計 43 件」と記載していたが、棚卸しの結果 **実際は 50 件**であることが判明。個別ケース番号（A-01〜E-05）を正とする。

### 優先度別

| 優先度 | 件数 |
|---|---|
| **高** | 26 件 |
| **中** | 17 件 |
| **低** | 7 件 |
| **合計** | **50 件** |

### 実施状況別（2026-06-21 時点）

| 区分 | 件数 | 内訳 |
|---|---|---|
| ✅ 実施済み PASS | **36 件** | A-01〜A-08, C-01〜C-06, C-09〜C-19, D-01〜D-06, E-01〜E-05 |
| ⬜ 未実施 | **14 件** | B-01〜B-05, C-07〜C-08, C-20〜C-25, D-07 |
| **合計** | **50 件** | |

### 未実施のうち優先度「高」（6件）

| # | テストケース |
|---|---|
| B-01 | 低確信度でオーナーにプッシュ通知が届く |
| B-02 | 通知メッセージに質問内容・確信度・AI回答案が含まれる |
| B-04 | エスカレーション後も `conversations` に記録される |
| C-20 | メニューを新規作成できる |
| C-21 | メニューを編集できる |
| C-22 | メニューを削除できる |

---

## 6. 既知の制限事項（テスト対象外）

- Claude API のレスポンス品質（どの確信度になるかはモデル依存）
- LINE Messaging API の配信遅延（LINEサーバー側の問題）
- Supabase の書き込みレイテンシ
- メニュー管理の `duration` / `category` / `is_active`（スコープ外）
